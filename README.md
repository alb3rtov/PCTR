# PCTR

## Compile and execute
Make sure you have `gnatmake` compiler installed in your system.

To compile you have to use Makefile and type `make` and to run the program use `make test`

## Clean directories
To clean the directory use Makefile and type `make clean`
